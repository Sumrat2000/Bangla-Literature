<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for profile mode, so that the
         Flutter tool can communicate with the running application. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
